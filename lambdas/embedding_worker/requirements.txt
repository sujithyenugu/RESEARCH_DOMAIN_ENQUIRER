# embedding_worker Lambda — Python dependencies
#
# boto3 is pre-installed in the Lambda Python 3.11 managed runtime;
# it is intentionally omitted here to keep the deployment package small.
#
# Pin these to the versions tested in CI.  Update with:
#   pip install --upgrade requests requests-aws4auth
#   pip freeze | grep -E "^(requests|requests-aws4auth)==" >> requirements.txt

requests>=2.31.0          # HTTP client for OpenSearch /_bulk and index management API calls
requests-aws4auth>=1.3.0  # AWS SigV4 request signing for the OpenSearch es service
